{
  "name": "@lihuacat/story-pipeline",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test $(find src -type f -name '*.spec.ts')",
    "build": "tsc -p tsconfig.json"
  }
}
